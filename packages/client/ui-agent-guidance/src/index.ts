/**
 * Global Agent-instructions settings surface, node half.
 * The Host behavior lives in `@deepseek-ai/dsh-agent-guidance`; this package
 * contributes only the browser settings page.
 */

/** Host plugin body; the browser half is discovered through `dsh.client`. */
export function apply(): void {}
